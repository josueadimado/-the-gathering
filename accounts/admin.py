from django.contrib import admin
from django.contrib.auth.admin import UserAdmin

# Register the default User model
# Django admin already includes User management, but we can customize it here if needed

